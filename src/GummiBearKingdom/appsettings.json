{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=GummiBearKingdom;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}

  //Next set up the app to use data migrations to create the database in project.json

  //by adding MS.EF.Tools to dependencies and tools

  //after project.json run build

  // once no error run dotnet ef migrations add Initial & ef database update in cmd line inside src/project
